Schema:
  type: object
  properties:
    type:
      type: string
      enum:
        - array
        - boolean
        - integer
        - number
        - object
        - string
    additionalProperties:
      oneOf:
        - $ref: '#/definitions/Schema'
      default: true
